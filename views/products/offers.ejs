<% include ../partials/homepage-header %>

    <div class="tz">
        <% include ../partials/user-left %>


            <div class="tz-2">
                <div class="tz-2-com tz-2-main">
                    <h4>Your Recharge Details</h4>
                    <div class="tz-2-main-com add-pr11" style="overflow:auto;">
                        <!-- <div class="tz-2-main-1">
                        <div class="tz-2-main-2"> <img src="images/icon/d1.png" alt="" /><span>All Listings</span>
                            <p>All the Lorem Ipsum generators on the</p>
                            <h2>04</h2> </div>
                    </div> -->





                        <!-- <form method="POST" action="/products/offers"> -->
                            <div class="row">
                                <div class="form-group">
                                <select class="form-control" name="product_id" id="industry">
                                    <option value="" disabled selected>Industry*</option>
                                    <% result.forEach(function(res) { %>
                                        <option value="<%= res.id %>">
                                            <%= res.product_name %>
                                        </option>
                                        <% }); %>
                                </select>
                                </div>
                                <div class="form-group">
                                <input type="text" name="offers_text" id="offer_txt" placeholder="offer text" class="form-control">
                                </div>
                                <div class="form-group">
                                <input type="text" name="percent" id="percent" placeholder="percent" class="form-control">
                                </div>
                                <!-- <input type="submit" value="submit"> -->
                                <button type="button" class="btn btn-default btn-block  pr-sub sub " style="color:#fff;">Submit</button>
                            </div>
                        <!-- </form> -->
                    </div>
                </div>
            </div>
            <% include ../partials/notification %>
    </div>
    <% include ../partials/home-footer %>

        <script type="text/javascript">
            var id;
            $('#industry').on("change", function () {
                id = $(this).val();
            });
            $('.sub').on("click", function () {
                var txt = $('#offer_txt').val();
                var per = $('#percent').val();
                $.post("/products/offers",{"product_id":id,"offers_text":txt,"percent":per},function(d){
                    console.log(d);
                    console.log(d.status);
                    if(d.status==200){
                        $('#offer_txt').val(" ");
                        $('#percent').val(" ");
                        alert("add offer successfully");
                    }
                });
            });
        </script>